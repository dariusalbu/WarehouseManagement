package org.example.model;

public class Consumables extends Product {
    public Consumables(int id, String product_name, int current_stock, int category_id) {
        super(id, product_name, current_stock, category_id);
    }
}
